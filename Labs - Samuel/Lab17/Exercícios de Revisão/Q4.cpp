/*

O fragmento de código abaixo é válido? Se sim, o que ele vai mostrar na tela?

cout << boolalpha;
cout << (cin >> x, x > 0) << endl;

Sim, o fragmento de código é válido. Se um número positivo for inserido como entrada,
a saída será "true". Caso contrário, se um número não positivo (zero ou negativo) for 
inserido, a saída será "false".

*/